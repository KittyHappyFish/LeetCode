class Solution:
    def isMatch(self, s: str, p: str) -> bool:
        if s == '': return True

        if len(p)>1:
            # if (s[0] != p[0]) and (p[0] != '.'): return False
            jug_1 = (s[0] == p[0]) or (p[0] == '.')
            jug_2 = 
            
            if p(1)=='*': net = p
            else: net = p[1:]
        else 